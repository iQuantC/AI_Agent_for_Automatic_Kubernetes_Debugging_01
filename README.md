# AI_Agent_for_Automatic_Kubernetes_Debugging_01
In this project, we build a practical AI-powered DevOps Agent that monitors Kubernetes cluster, collects logs from pods, stores knowledge in a vector DB, and uses an AI Agent to diagnose the issues, suggest fixes and apply  the fixes automatically.
